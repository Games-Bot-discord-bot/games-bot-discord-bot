import { RESTError } from "discord-api-types/v10";
interface APIError {
    code: number;
    message: string;
}
export declare class WebhookError extends Error implements RESTError {
    code: number;
    message: string;
    constructor(data: APIError);
}
export {};
